import java.awt.*;
import java.awt.event.*;

class public Slip20B extends Frame implements windowListener
{
    Slip20B()
    {
        Color c = new Color.red;
        setColor(c);
        setvisible(true);
        setSize(400,400);
        setLayout(new FlowLayout());
    }
}